import type { ParsedArguments } from "./types.js";

/** Implement exactly the invocation grammar in CONTRACT.md. */
export function parseArguments(_argv: readonly string[]): ParsedArguments {
  throw new Error("fonte_cli_frame_incomplete");
}
